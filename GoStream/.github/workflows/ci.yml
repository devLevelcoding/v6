name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
      - run: go vet ./...
      - run: go build ./...
      - name: race
        run: go test -race -count=1 ./...
      - name: static binary (CGO_ENABLED=0)
        run: CGO_ENABLED=0 go build -trimpath -ldflags '-s -w' -o /dev/null ./cmd/gostreamd
      - name: bench smoke
        run: go test -run '^$' -bench . -benchtime 1x ./...

  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.26'
          cache: true
      - name: fuzz RFC 6455 frame parser
        run: go test -run '^$' -fuzz 'FuzzReadMessage$' -fuzztime 60s ./internal/ws
